### Database

```sh
This is an API containing the data for the rock-artstock.com site.
```

### Running the API

```sh
In order to run the API, within the db folder, 
create a .db file titled artdatabase.db.
```


### Routs

```sh
All routes also have an id value.

awards (description)
commissions (description)
events (name, start, end)
calendar (name, start, end)
items(style, project, item)
stockList (style, project, item)
pages (title, subtitle, image, link, flex)
styles (style, description, image_1, image_2, image_3)
homeCarousel (image)
galleries (style, description, link)
collage (description, image, completed)
coloredPencil (description, image, completed)
paintings (description, image, completed)
photography (description, image, completed)
prints (description, image, completed)
otherProjects (description, image, completed)

optional: tabs(title, image, text)

```